# AI CI Automation - Required Secrets
#
# These values should be added as GitHub repository secrets:
#   Settings → Secrets and variables → Actions → New repository secret
#
# They are NOT environment variables — do not commit real values.

# GitHub Personal Access Token (fine-grained)
# Create at: https://github.com/settings/tokens?type=beta
# Required permissions: Contents (rw), Issues (rw), Pull requests (rw), Actions (r), Workflows (rw)
# Why not GITHUB_TOKEN? The built-in token cannot trigger other workflows.
# Why not GITHUB_PAT? GitHub blocks secrets starting with GITHUB_.
GH_PAT=github_pat_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Claude Code authentication (configure ONE of these)
#
# Option 1: OAuth Token (recommended)
# Get via: npm install -g @anthropic-ai/claude-code && claude setup-token
CLAUDE_CODE_OAUTH_TOKEN=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
#
# Option 2: Anthropic API Key
# Get from: https://console.anthropic.com/
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
